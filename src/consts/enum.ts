export enum Status {
    HOST = 'host',
    GUEST = 'guest',
    OBSERVER = 'observer'
}

export enum StatusWithoutHost {
    GUEST = 'guest',
    OBSERVER = 'observer'
}

export enum OauthType {
    KAKAO = 'kakao',
    NAVER = 'naver',
    FACEBOOK = 'facebook'
}